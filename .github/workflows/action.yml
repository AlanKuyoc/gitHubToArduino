name: Push Buzzer

on:
  push:

jobs:
  notify-arduino:
    runs-on: ubuntu-latest

    steps:
      - name: Enviar señal a Processing
        run: |
          curl -X POST http://89.116.55.194:5000
