name: Push Buzzer

on:
  push:

jobs:
  notify-arduino:
    runs-on: ubuntu-latest

    steps:
      - name: Enviar señal a Processing
        run: |
          curl -X POST http://TU-IP-PUBLICA:5000
